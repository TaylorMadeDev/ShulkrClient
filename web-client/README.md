# Shulkr Web Client

Standalone browser client for the Shulkr Express backend.

## Run

```bash
npm install
npm run dev
```

Open `http://127.0.0.1:5177`.

The app expects the backend at `http://127.0.0.1:50991` by default. Change it from the Settings page if needed.
